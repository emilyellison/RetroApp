<%= form_for @note, remote: true, html: { id: "new_#{category.name.downcase}_note" } do |f| %>
	<%= f.hidden_field :category_id, value: category.id %>
	<%= f.hidden_field :retro_id, value: retro.id %>
	<%= f.label :description %>
	<%= f.text_field :description %>
<% end %>