<% if @team.errors.any? %>
	<% @team.errors.full_messages.each do |msg| %>
		<p><%= msg %>.</p>
	<% end %>
<% end %>

<%= form_for @team do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit %>
<% end %>